type ShoppingCartProps = {
  display: boolean
}

const ShoppingCart = ({ display }: ShoppingCartProps) => {
  return ( 
    <div>
      <h1>Shopping Cart</h1>
    </div>
   );
}
 
export default ShoppingCart;